export default class Comment{
    constructor( profileName, commentText, timestamp){
        this.profileName = profileName;
        this.commentText = commentText;
        this.timestamp = timestamp;
    }
}